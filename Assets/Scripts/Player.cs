using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{
    //To jest komentarz 
    // Start is called before the first frame update
    void Start()
    {
        //tu jest jakis kod i on co? robi
    }

    // Update is called once per frame
    void Update()
    {
        Start();
        Start();
    }

}
